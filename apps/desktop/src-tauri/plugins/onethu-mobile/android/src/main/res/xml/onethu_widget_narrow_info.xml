<?xml version="1.0" encoding="utf-8"?>
<!--
  宿主小组件「窄条 2×1」的元信息（初始占位 2 列 × 1 行）：只画一行内容与脚注（标题在这一形态里让位，见 OnethuWidget.kt 的紧凑渲染）。

  - 与标准形态共用同一套布局（@layout/onethu_widget）：行数由实际高度决定，故这里只需要
    声明初始占位与可拖动的下界；放置后用户仍可自由改尺寸。
  - minResizeWidth/Height 统一 110×40dp：放开到「2 列 × 1 行」以便拖成窄条。
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="110dp"
    android:minHeight="40dp"
    android:targetCellWidth="2"
    android:targetCellHeight="1"
    android:minResizeWidth="110dp"
    android:minResizeHeight="40dp"
    android:updatePeriodMillis="1800000"
    android:initialLayout="@layout/onethu_widget"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:description="@string/onethu_widget_desc" />
